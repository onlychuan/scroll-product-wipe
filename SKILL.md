---
name: scroll-product-wipe
description: 使用用户提供的图片制作滚动分屏、产品原位对齐、背景边界自下而上擦换的产品展示网页，并向用户提供八种图片承接提示。本技能不生成图片。适用于 Cyclemon 风格产品状态切换和拆解展示。
---

# 滚动分屏与产品边界擦换

## 效果定义

滚动推动下一屏背景从底部进入。边界经过产品时，上方仍显示上一状态，下方已经显示下一状态。每屏的背景和产品使用同一裁切边界；整组图层固定在视口内，由滚动改变遮罩。

当前效果的核心约束：同一产品处于屏幕中心，保持相同视角、比例和定位，仅切换状态。保持滚动与边界位置直接对应，反向滚动能够原路恢复。状态切换无需真实三维模型。

## 工作流程

1. 优先读取用户现有页面和素材；提供了参考视频时先看关键帧及转场中间帧，确认边界方向和产品对齐方式。
2. 将 [图片素材提示](references/image-continuity.md) 提供给用户，帮助用户自行准备或生成图片。八项是可选择、可组合的图片承接方式，不能理解成必须实现八段动画。
3. 只使用用户提供的图片。本技能不包含图片生成功能，不调用图片生成工具。缺少素材时，告诉用户需要补充哪些状态，以及统一画布、锚点、视角和比例的要求；可先用仓库案例展示机制，但明确标为案例，不能冒充用户产品。
4. 参考 [页面模板](assets/template.html) 实现。替换模板内 `states` 的图片路径、背景色和无障碍描述；按现有页面框架移植也可以。
5. 图片数量以用户最后选择为准，从状态数组推导图层数和滚动距离。默认无可见文案；只有用户需要时才加入字幕，并让字幕属于对应场景。

本仓库 [墙插案例](examples/outlet/index.html) 包含现有四张透明产品图和可直接打开的完整页面，用于理解效果与替换素材。

## 关键实现

- 外层提供滚动距离，内部舞台使用 `position: sticky; top: 0`，高度为一屏。
- 所有场景绝对定位、覆盖同一舞台，按先后顺序叠放；后面的场景在上层。
- 每层包含该状态的完整背景与产品图。产品使用统一的容器、宽高比和居中规则。
- 令 `H` 为舞台实际高度，`q = clamp(-journey.top / H, 0, N)`。第 0 层始终完整可见；其余第 `i` 层的顶部裁切百分比为 `clamp((i - q) * 100, 0, 100)`。
- 使用 `clip-path: inset(top% 0 0 0)`。在 `q = i - 0.5` 时，新层覆盖舞台下半部，旧层保留在上半部。
- 外层高度可设为 `(N + 1) * H`，留一屏滚动距离停留在最终状态。最后一层不再被裁空。
- 使用被动滚动监听和 `requestAnimationFrame` 合并更新；不要对裁切值添加滞后缓动或 CSS transition。
- 使用稳定的视口高度单位，并在尺寸变化后重新读取舞台高度。尊重减少动态效果的系统偏好，关闭自动平滑滚动和附加运动。

## 素材对齐

透明画布中心一致不代表产品对齐。选取跨状态仍存在的部位作为锚点，例如后壳中心或安装孔；检查它在各图中的坐标与尺寸。拆解时某些部件消失，不能按剩余可见像素重新居中、自动裁边或独立放大。

产品采用透明 PNG/WebP；背景由场景层控制。检查透明通道，不能把棋盘格图片当透明图。若使用含环境的完整图片，环境和产品也要一起裁切，并保持合理的画面接续。

## 避免的偏离

- 用交叉淡化、部件上下漂移或单纯背景渐暗替代边界擦换。
- 背景边界与产品遮罩各自运行，导致两者不同步。
- 滚动时给所有状态分别设置不同缩放和位移，破坏原位衔接。
- 将内部拆解自动扩展成上墙安装流程。仅当用户明确要求时增加安装叙事。
- 把八种素材规则都强制塞入一个项目，或默认添加字幕、品牌口号、结尾屏。

## 用户要求验证时

检查每个转场的起点、中点、终点及反向滚动。中点应清楚看到上旧下新，边界同时穿过背景和产品。检查首尾状态、手机比例、图片缺失、页面中途刷新和调整窗口大小。无文案版本仍保留图片替代文本及导航无障碍名称。

交付说明使用了哪些状态和哪些承接规则。发布和上传按当前用户授权处理，skill 本身不代表公开发布许可。
